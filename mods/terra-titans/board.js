// Terra Titans Mod — Board.
//
// Reuses Dominion's default 40-space loop board + color groups as the fallback board
// (used when no atlas world is the active map). The headline experience is the globe
// world (worlds: [TERRA_TITANS]); this loop is the classic-style fallback.
export { BOARD_SPACES, COLOR_GROUPS } from '../dominion/board';
